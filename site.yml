---
- hosts: all
  become: true
  become_user: root
  become_method: su

  roles:
    - apt
    - update
    - ntp
    - sshd
    - postfix
    - pam
    - sudo
    - groups
    - packages
    - sysctl
    - grub
    - home_fix
    - debsecan
    - fail2ban
    - logwatch
    - ssh_key
    
    ##
    # Virtual machine under QEMU?
    ##
    #- qemu_guest

    ##
    # Paket filter (Firewall)
    ##
    #- ufw
    #- nftables

    ##
    # Services
    ##
    #- docker
    #- libvirt

    ##
    # Run aide task after everything 
    # is installed
    ##
    - aide

    - reboot